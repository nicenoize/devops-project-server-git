apiVersion: v1
kind: Service
metadata:
  name: todo-backend-service
spec:
  ports:
  - port: 3000 # application port
    targetPort: 3000 # container port
  selector:
    app: todo-server-app
  type: LoadBalancer
